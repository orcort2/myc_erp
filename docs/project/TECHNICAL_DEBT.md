> Estado: VIGENTE
>
> Tipo: Vigente (canónico)
>
> Autoridad: Alta para deuda técnica actual
>
> Prevalece sobre: propuestas de refactorización o riesgos contenidos en auditorías y documentos históricos
>
> Corte verificado: 2026-08-26

# Deuda técnica vigente

No incluye funcionalidades futuras. Cada elemento corresponde a una condición presente que afecta seguridad, integridad, mantenimiento, operación o UX del alcance implementado.

| ID | Prioridad | Impacto actual | Archivos afectados principales | Propuesta de solución |
| --- | --- | --- | --- | --- |
| TD-008 | P1 | La autoridad documental PDF ya converge en el renderer backend declarativo versionado y congela el artefacto final; la presentación React y la validación visual/física de todas las familias todavía pueden diferir del documento. | `FieldSheetLayout.jsx`, `fieldSheetPagination.js`, `field_sheet_engine_pdf.html`, servicios PDF | Validar visualmente las familias restantes y alinear la previsualización sin convertir React/Mobile en autoridad documental ni romper históricos. |
| TD-009 | P1 | Semántica y automatizaciones incompletas permiten captura manual no validada. | definiciones de plantillas, servicios de Hojas de Campo, metrología/incertidumbre | Cerrar el registro canónico, conectar motores aprobados y probar cada familia con casos reales. |
| TD-010 | P1 | Los archivos genuinamente no identificados carecen de resolución formal y carga/envío/descarga XLSX no tienen E2E HTTP/browser automatizado. | servicios de paquetes/readiness, UI Captura y Calidad | Agregar resolución explícita y E2E autenticado; persistencia, readiness, transición, acceso XLSX y detección semántica del servicio ya tienen pruebas y caso real verificable. |
| TD-011 | P1 | Cerrar borrador fiscal puede perder cambios; pagos y CxC ya reutilizan el controlador, pero notas de crédito y la experiencia documental especializada siguen incompletas. | `useInvoiceWorkbenchController.js`, `frontend/src/components/invoice-workbench/*` | Autosave/confirmación de descarte; completar únicamente las capacidades restantes reutilizando el controlador único. |
| TD-012 | P1 | Producción, cancelación/sustitución, PPD y notas fiscales no cierran el ciclo fiscal existente. | servicios Facturama, invoices, modelos y UI | Diseñar estados idempotentes sobre el agregado actual, persistir respuestas/documentos y cubrir Sandbox antes de habilitar Producción. |
| TD-013 | P1 | Falta E2E browser exhaustivo por todos los roles, aunque la matriz, navegación, administración multirrol y acciones principales ya consumen permisos efectivos; PortalMembership y su UI ya están integrados. | `frontend/src/constants/navigation.js`, Settings, Portal y suites E2E futuras | Automatizar recorridos browser por cada capacidad sin duplicar autoridad frontend. |
| TD-015 | P1 | Compatibilidad de OT/firmas y catálogos fiscales mantiene fuentes conceptuales dobles. | modelos `service_order.py`, `invoice.py`, migraciones y consumidores | Medir uso, migrar históricos, encapsular lectura legacy y retirar columnas sólo con migración reversible. |
| TD-016 | P2 | Páginas monolíticas, componentes duplicados, alerta nativa y bundle grande elevan regresiones/tiempo de carga. El controlador del Workbench ya fue extraído de `BillingPage`, pero permanecen los demás focos. | `ServiceOrdersPage.jsx`, `QuotationsPage.jsx`, `ClientsPage.jsx`, `ServiceOrderSignatureMorph.jsx`, labs/workbench, Vite | Continuar extrayendo dominios, usar `ConfirmDialog`, consolidar componentes y aplicar lazy loading/code splitting con pruebas visuales; no revertir el controlador compartido de Facturación. |
| TD-017 | P2 | Los puertos no tienen una fuente única y pueden divergir entre Toolkit, frontend y documentación. | `scripts/config.sh`, scripts status/start y documentación | Centralizar puertos y probar comandos no destructivos; el forwarding de Doctor ya fue corregido y tiene diagnóstico LibreOffice verificable. |
| TD-018 | P2 | CORS continúa duplicado y el almacenamiento sigue siendo local; ETAPA 3 ya centralizó rutas, validación, escritura atómica, entrega y exclusión de datos operativos de Git. Falta backend durable/replicado y prueba de despliegue limpio. | `backend/app/main.py`, `backend/app/core/config.py`, `backend/app/services/storage_service.py` | Centralizar CORS, seleccionar persistencia durable y probar despliegue/recuperación en entorno limpio. |
| TD-019 | P2 | No hay pipeline reproducible de validación/despliegue ni cobertura suficiente de seguridad y flujos críticos. | configuración del repositorio y suites backend/frontend | Añadir CI con build, pruebas, `alembic heads/current`, auditoría de rutas y E2E autenticados no destructivos. |
| TD-020 | P2 | Registro de campos de Hojas de Campo refiere al MDE futuro y puede confundirse con implementación. | `docs/architecture/FIELD_SHEET_FIELD_REGISTRY.md`, definiciones frontend/backend | Mantener el registro como contrato vigente independiente y marcar MDE sólo como consumidor futuro opcional. |
| TD-022 | P1 | Decisiones de seguridad del Motor persistidas antes de Fase 7 pueden conservar su hash y vínculos exactos sin la base canónica completa necesaria para recalcularlo. Auditoría las identifica como `asserted`, no como `verified`; inventar o inferir permisos históricos sería incorrecto. | `resolution_security_decisions`, adaptador de auditoría y registros históricos | Mantener la distinción explícita; si existe una fuente institucional firmada para un histórico concreto, incorporar su base mediante una migración trazable, nunca por inferencia. Las decisiones nuevas ya conservan `context_snapshot.evidence_payload`. |
| TD-024 | P1 | El desbloqueo bloquea correctamente cualquier ETS con operación, pero todavía no existe una estrategia no destructiva para actualizar esos expedientes. | `quotation_service_changes.py`, `service_order_rebuilds.py`, UI de Cotizaciones | Diseñar una excepción futura separada; no relajar ni reutilizar la reconstrucción física. |
| TD-025 | P2 | Ventas/catálogo carecen todavía de E2E autenticado en navegador para el recorrido desbloqueo→delta→reconstrucción y para alta de empresa “Otro”. | `QuotationsPage.jsx`, endpoints de catálogo/desbloqueo | Agregar pruebas browser con PostgreSQL representativo sin duplicar el controlador de revisión. |
| TD-026 | P1 | Varias mutaciones autorizadas fuera de ETS todavía no propagan el actor y sus servicios aceptan `user_id=None`, debilitando la trazabilidad institucional. ETS y el lifecycle humano de Certificados exigen actor; `create_certificate` conserva actor opcional porque también es una derivación automática de Equipos/Resoluciones. Permanecen Clientes, Catálogo y Equipos. | routers/servicios de Clientes, Catálogo y Equipos; `audit_logs` | Inyectar identidad en las mutaciones restantes y agregar pruebas HTTP sobre `audit_logs`, sin romper operaciones técnicas legítimas. |
| TD-027 | P1 | El gate queda verde y el bootstrap cubre los permisos del inventario, pero 32 claves HTTP difieren literalmente del snapshot técnico después de consolidar la familia HTTP Mobile bajo `mobile.access`; se agregaron las cuatro capacidades temporales `lab_work_order_groups.*` además de las familias vigentes de Mobile, Venta y Reparación. | `scripts/validate_capability_catalog.py`, inventario HTTP, contratos Venta/Reparación/Mobile y Catálogo Funcional | Estado BLOQUEADO POR DECISIÓN: revisar institucionalmente cada familia restante antes de incorporarla al catálogo permanente; no retirar capacidades vigentes mientras sus verticales las consuman. |
| TD-028 | P1 | El frontend conserva advisories altos en `nanoid` y `postcss`. | `frontend/package-lock.json` | Actualizar dependencias de forma controlada y repetir auditoría, tests y build. |
| TD-029 | P2 | El startup de FastAPI inserta permisos/roles base del Portal y hace commit; el despliegue tiene una mutación de catálogo implícita. | `backend/app/main.py`, `services/portal/permission_service.py` | Mover la siembra a un bootstrap/migración explícito, idempotente y observable. |
| TD-030 | P1 | La liberación individual usa `release_to_client`, pero el lote ETS conserva un loop transaccional propio con la misma decisión y puede divergir en futuros cambios. No autentica ni fusiona la compuerta financiera. | `backend/app/services/certificates.py`, routers ETS/Certificados | Hacer que el lote delegue en una operación propietaria sin commit interno durante un sprint separado, preservando resultado parcial y actor. |
| TD-031 | P1 | El núcleo ETS múltiple/evolucionado ya congela identidad/configuración y evita fallback a Servicio General, pero no tiene UI de cards/tabs, deep-link por notificación, E2E browser ni workflows técnicos de categoría. | `service_execution.py`, `service_orders.py`, `api.js`, futura UI ETS | Revisar Fase 1, componer el board sin duplicar entidades y abrir cada workflow en fases explícitas posteriores. |
| TD-032 | P1 | `ServiceTask` sólo materializa el atajo idempotente `#tarea`; faltan lifecycle de edición/cierre, fechas, reasignación y políticas de visibilidad. | modelos/servicio de ejecución y Activity | Diseñar el contrato transversal de tareas sobre la entidad existente, sin convertir Activity en gestor de tareas ni agregar NLP. |
| TD-033 | P1 | Las decisiones por partida tienen origen interno; portal/app aún no aportan autenticación de intención, clave idempotente externa ni conciliación de respuestas inciertas. | Cotizaciones, Portal y futuro cliente móvil | Implementar adaptadores sobre `QuotationItemDecision`, con ownership, idempotencia y conciliación; no crear otra máquina comercial. |
| TD-034 | P2 | El grafo referencial ETS/comercial es íntegro, pero Alembic advierte que no puede ordenar globalmente el ciclo `ServiceStage ↔ QuotationItemDecision/QuotationItem ↔ TechnicalServiceRequest/ServiceUnit`; hoy `alembic check` queda limpio, pero una versión futura de SQLAlchemy podría endurecer el warning. | modelos/migración Fase 1 y `migrations/env.py` | Evaluar `use_alter`/nombres explícitos o exclusión dirigida para autogenerate sin retirar FKs ni degradar reconstrucción histórica. |
| TD-035 | P1 | El contexto Mobile internal/client y el scope externo LAB ya existen, pero se aceptan temporalmente JWT internos emitidos antes del cambio y no hay revocación individual por JTI. Las rutas productivas Mobile siguen bloqueadas para clientes porque aún no tienen contratos de ownership externo aprobados. | `core/mobile/security.py`, `mobile_technician.py`, MYC Mobile | Retirar la compatibilidad sólo después de caducar builds/sesiones antiguas; diseñar revocación y exponer cada recurso productivo en fases separadas con pruebas IDOR. |
| TD-036 | P1 | Portal/app todavía no exponen un endpoint de decisión por partida con identidad de cliente, ownership de organización/cotización, idempotencia externa y conciliación. La ruta interna rechaza esos orígenes y sólo deriva `internal`. | futuro adapter de portal/app sobre `QuotationItemDecision` | Diseñar el contrato explícito en una fase posterior; no aceptar `source`, `client_id` ni ownership desde payload y no reutilizar el endpoint interno. |
| TD-037 | P1 | La versión operativa anterior de OT LAB fue validada físicamente, pero Tickets/filtros/reapertura/revisiones, eliminación y Fase 3 recepción→FieldSheets→cierre aún no tienen repetición Android/iPhone/TestFlight ni pruebas automatizadas renderizadas de componentes React Native. Estados, contexto grupal/individual, doble firma local/un POST, read-only posterior a recepción, navegación pura, doble submit y lifecycle backend sí tienen regresión. Falta validar físicamente revisión completa, elección grupal/individual, pointerdown/move/up/postMessage, orientación, teclado, transición a captura, selección posterior de hermanas, PDF y carrera canvas/ScrollView. | `myc-mobile`, servicios LAB/Tickets y contratos | Ejecutar checklist físico nuevo —grupo parcial, ambas modalidades, dos firmas, POST, `received_signed`, primera/última FieldSheet, `ready_to_close`, cierre/PDF, preserve/invalidate, portrait/landscape, stroke/scroll/teclado/Back, refetch/hermanas y errores 409/422/500/red— antes de declarar cierre. |
| TD-038 | P1 | Notifications V1 persiste antes de Expo y tolera fallos, pero el intento sigue en la petición, sin cola durable, retry ni consulta de receipts; `push_delivered_at` sólo representa aceptación de Expo. Tampoco existen preferencias, quiet hours o agrupación. | `push_notifications.py`, `Notification`, `PushDevice`, MYC Mobile | Validar físicamente iOS/Android y diseñar en fase propia una cola genérica/retry/receipts y preferencias; no reutilizar el worker del Motor ni volver el push fuente de verdad. |
| TD-039 | P1 | `myc-mobile` reporta 20 advisories npm (11 moderados y 9 altos) al reinstalar exactamente el lock de Expo SDK 54; no se aplicó un fix mayor automático. | `myc-mobile/package-lock.json` | Auditar rutas transitivas, actualizar de forma compatible con Expo y repetir TypeScript, lint, Expo Doctor y pruebas físicas antes de release. |
| TD-040 | P2 | La eliminación de OT saca archivos de rutas publicables antes del commit y los restaura ante rollback; si el `unlink` final del staging falla por una condición del filesystem, el dato queda inaccesible pero permanece en `.pending-deletions` sin barrido operativo automatizado. | `backend/app/services/service_orders.py`, `STORAGE_ROOT/.pending-deletions` | Incorporar un barrido idempotente y observable de staging huérfano, con antigüedad mínima y alerta, dentro del mantenimiento institucional de storage. |
| TD-042 | P1 | Comunicaciones A–I tiene regresión automatizada, bundle y concurrencia PostgreSQL, pero todavía no fue aceptada en dos dispositivos físicos iOS/Android con push, deep link, background/foreground, typing, recibos y escritura simultánea. | `myc-mobile`, backend Communications/realtime y cierre A–I | Ejecutar el checklist físico multi-dispositivo antes de aceptar el frente; conservar REST como recuperación y no declarar push como entrega canónica. |
| TD-043 | P1 | ETS Venta tiene regresión backend, build web y validación estática móvil, incluido endurecimiento tester, pero no aceptación autenticada física para los tres resultados de garantía, firma Portal y atestación de entrega técnica. | `SaleEtsTab.jsx`, MYC Mobile, Portal y servicios Venta | Ejecutar checklist E2E con PostgreSQL representativo y perfiles asesor/admin/técnico/cliente; mantener EN REVISIÓN hasta validarlo. |
| TD-044 | P2 | La nota PDF es funcional pero básica; no existe plantilla institucional avanzada ni integración con rastreo/mapas/correo externos. | `sale_execution.py`, configuración institucional futura | Definir plantilla y conectores en una fase explícita; un proveedor externo no debe ser autoridad del estado de entrega. |
| TD-045 | P1 | ETS Mantenimiento tiene regresión automatizada, migración y build web, pero falta aceptación autenticada en laboratorio/campo, firma y fotografías desde dispositivo físico. | `MaintenanceEtsTab.jsx`, servicios/API Mantenimiento y PDF | Ejecutar checklist E2E con asesor, técnico, autorizador, firmante y cierre antes de aceptar el vertical. |
| TD-046 | P2 | Materiales de Mantenimiento conservan un contrato extensible, pero aún no existen Compras/Almacén, existencias, ubicaciones, lotes, movimientos ni escaneo móvil. | `maintenance_materials` y catálogo | Integrar módulos futuros sin convertir el ETS ni el costo interno en autoridad de inventario. |
| TD-047 | P2 | El patrón de bloqueantes UX accionables existe en Mantenimiento, pero Calibración/Hojas de Campo aún puede presentar faltantes sin navegación o resaltado exacto. | ETS Calibración y Hojas de Campo | Adoptar `severity/message/section/field` en un sprint específico sin rehacer su selector de hojas. |
| TD-048 | P1 | Los conceptos legacy cuya categoría estructurada no coincide con ninguna clave conocida pueden conservar una identidad histórica ambigua; reclasificarlos por Tipo, nombre o descripción volvería a introducir heurísticas. | `catalog_items.operational_category`, migración `e2a4c6d8f0b1` y catálogo | Revisar esos conceptos con decisión humana y actualización explícita; no propagar cambios a snapshots, cotizaciones ni ETS históricos. |
| TD-049 | P1 | Verificación reutiliza correctamente el pipeline y el retorno ZIP congela automáticamente el Master real registrado, pero aún no existe evidencia de un Master institucional real ni E2E autenticado/browser con un ETS mixto. Varias etiquetas persistentes heredadas (`calibration_date`, estado `calibrated`) permanecen por compatibilidad. | Control Documental, Hojas de Campo, Captura, Calidad, Certificados y `ServiceOrdersPage.jsx` | Registrar el Master oficial con identidad estructurada `service_type=verification`, ejecutar el recorrido físico completo y decidir de forma compatible si la presentación requiere etiquetas neutrales; no crear tablas ni estados paralelos. |
| TD-050 | P3 | Auditoría Anual es una capacidad transversal futura aún no implementada. Deberá conservar apertura/cierre, ciclo, auditor/organismo, documentos/evidencias, observaciones, hallazgos, no conformidades, acciones correctivas, responsables, compromisos, seguimiento, eficacia y conclusiones; estudiar integración con Tickets, Motor de Resoluciones, Activity, Documentos y permisos. Una auditoría cerrada será histórica e inmutable. El eventual cambio de ciclo sólo podrá reiniciar contadores/consecutivos/acumuladores declarados reiniciables, nunca borrar ETS, certificados, facturas, OT, auditorías, Activity, evidencias ni trazabilidad. Debe distinguir explícitamente `RESET DE CONTADORES` de `BORRADO DE DATOS`; queda por decidir si el reinicio será anual automático o condicionado al cierre formal. | Diseño futuro transversal; sin dependencia del motor actual de folios | Diseñar e implementar por fases finales. No conectar todavía folios ni resets a Auditoría Anual y no reutilizar el reset destructivo de desarrollo. |
| TD-052 | P1 | Herramientas administrativas ETS sólo aceptan ID explícito y bloquean expedientes con actividad; faltan búsqueda por folio/cliente/cotización, reparación estructural no destructiva y doble aprobación configurable para riesgo CRITICAL. | Centro de Resoluciones, integración `service_order_administration`, auditoría 2026-08-25 | Extender el registro con resolvers de búsqueda y diseñar cada reparación sobre snapshots/evidencia; no relajar bloqueantes ni usar heurísticas. |
| TD-053 | P1 | El bootstrap conserva `Administrador: *`; las nuevas herramientas declaran permisos exactos pero el wildcard histórico todavía puede satisfacerlos. | `backend/app/core/permissions.py`, catálogo institucional de capacidades | Sustituir el wildcard sólo dentro de una fase RBAC aprobada, con matriz completa y E2E por rol; no introducir bypass adicional. |
| TD-054 | P2 | La revisión Mobile genera upgrade/downgrade SQL offline correctamente cuando se acota `c4e0ead1af28:d6f2a4c8e0b1`, pero una generación offline completa `base→head` tropieza antes en la migración histórica `d4e5f6a7b8c9`, que intenta consultar mediante un bind inexistente en modo `--sql`. El upgrade online y `alembic check` permanecen correctos. | `backend/migrations/versions/d4e5f6a7b8c9_add_work_orders_and_field_sheet_templates.py`, Alembic offline | Diseñar una estrategia de exportación offline compatible sin editar historia aplicada; conservar como autoridad de despliegue el ciclo online/backup validado mientras se resuelve. |

## Criterio de retiro

Una deuda se elimina sólo cuando la condición deja de existir y la validación queda registrada. Si la solución propuesta cambia el alcance funcional, primero debe actualizarse [`CURRENT_SCOPE.md`](CURRENT_SCOPE.md); no se debe convertir una mejora futura en deuda vigente.

## Deuda agregada 2026-08-26

- TD-055 (P1): QA físico de solicitud/claim/decisión/realtime con Sr/Jr/Viewer/staff.
- TD-056 (P2): realtime por rooms de usuario en hub in-memory; la fase multi-worker deberá adoptar transporte compartido sin canal paralelo.
- TD-057 (P2): una `LabWorkOrderGroupRequest` aprobada cuyo grupo fue eliminado conserva los folios originales en el evento estructurado `lab_work_order.group_materialized` y en la conversación, pero su proyección `LabWorkOrderGroupRequestRead.folios` devuelve `[]` al quedar `root_work_order_id=NULL`. Decidir si la consulta histórica necesita un snapshot estructurado propio y, sólo entonces, diseñar su migración; no reconstruir desde huecos ni reutilizar folios.
